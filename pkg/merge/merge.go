package merge

// Merge merges all the passed documents together using the traversal as a pattern for the final document
func Merge(traversal interface{}, documents []interface{}) interface{} {
	return nil
}
